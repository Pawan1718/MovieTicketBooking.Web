@* @model MovieTicketBooking.Web.ViewModels.HomeViewModels.AvailableTicketViewModel

@{
    ViewData["Title"] = "Available Tickets";
}
<style>
    .ticket-form {
        margin-bottom: 20px;
    }

        .ticket-form .row {
            margin-bottom: 10px;
        }

        .ticket-form .form-check {
            display: flex;
            align-items: center;
        }

        .ticket-form .form-check-input {
            margin-right: 5px;
        }

        .ticket-form .form-check-label {
            margin-bottom: 0;
        }

    .screen {
        width: 100%;
        max-width: 600px; 
        height: 50px; 
        background-color: #fff;
        border: 2px solid #333;
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
        margin: 0 auto 20px;
    }
</style>

<h1>@ViewData["Title"]</h1>

<div class="container">
    <div class="screen"></div>

    <div class="row">
        <div class="col-md-6">
            <h2>Block A</h2>
            <form asp-action="BookTickets" asp-controller="Home" method="post" class="ticket-form">
                @for (var row = 1; row <= 5; row++)
                {
                    <div class="row">
                        @for (var col = 1; col <= 5; col++)
                        {
                            var seatNumber = (row - 1) * 5 + col;
                            if (Model.BlockASeats.Contains(seatNumber))
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" name="selectedSeats" value="@seatNumber" id="seat_A_@seatNumber" />
                                        <label class="form-check-label" for="seat_A_@seatNumber">Seat A#@seatNumber</label>
                                    </div>
                                </div>
                            }
                            else
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" disabled />
                                        <label class="form-check-label">Unavailable</label>
                                    </div>
                                </div>
                            }
                        }
                    </div>
                }
                <input type="hidden" asp-for="ShowtimeId" />
                <button type="submit" class="btn btn-outline-success">Book Selected Seats</button>
            </form>
        </div>
        <div class="col-md-6">
            <h2>Block B</h2>
            <form asp-action="BookTickets" asp-controller="Home" method="post" class="ticket-form">
                @for (var row = 1; row <= 5; row++)
                {
                    <div class="row">
                        @for (var col = 1; col <= 5; col++)
                        {
                            var seatNumber = (row - 1) * 5 + col;
                            if (Model.BlockASeats.Contains(seatNumber))
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" name="selectedSeats" value="@seatNumber" id="seat_B_@seatNumber" />
                                        <label class="form-check-label" for="seat_B_@seatNumber">Seat B#@seatNumber</label>
                                    </div>
                                </div>
                            }
                            else
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" disabled />
                                        <label class="form-check-label">Unavailable</label>
                                    </div>
                                </div>
                            }
                        }
                    </div>
                }
                <input type="hidden" asp-for="ShowtimeId" />
                <button type="submit" class="btn btn-outline-success">Book Selected Seats</button>
            </form>
        </div>
        <div class="col-md-6">
            <h2>Block C</h2>
            <form asp-action="BookTickets" asp-controller="Home" method="post" class="ticket-form">
                @for (var row = 1; row <= 5; row++)
                {
                    <div class="row">
                        @for (var col = 1; col <= 5; col++)
                        {
                            var seatNumber = (row - 1) * 5 + col;
                            if (Model.BlockASeats.Contains(seatNumber))
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" name="selectedSeats" value="@seatNumber" id="seat_C_@seatNumber" />
                                        <label class="form-check-label" for="seat_C_@seatNumber">Seat C#@seatNumber</label>
                                    </div>
                                </div>
                            }
                            else
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" disabled />
                                        <label class="form-check-label">Unavailable</label>
                                    </div>
                                </div>
                            }
                        }
                    </div>
                }
                <input type="hidden" asp-for="ShowtimeId" />
                <button type="submit" class="btn btn-outline-success">Book Selected Seats</button>
            </form>
        </div>
        <div class="col-md-6">
            <h2>Block A</h2>
            <form asp-action="BookTickets" asp-controller="Home" method="post" class="ticket-form">
                @for (var row = 1; row <= 5; row++)
                {
                    <div class="row">
                        @for (var col = 1; col <= 5; col++)
                        {
                            var seatNumber = (row - 1) * 5 + col;
                            if (Model.BlockASeats.Contains(seatNumber))
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" name="selectedSeats" value="@seatNumber" id="seat_D_@seatNumber" />
                                        <label class="form-check-label" for="seat_D_@seatNumber">Seat D#@seatNumber</label>
                                    </div>
                                </div>
                            }
                            else
                            {
                                <div class="col-md-2">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" disabled />
                                        <label class="form-check-label">Unavailable</label>
                                    </div>
                                </div>
                            }
                        }
                    </div>
                }
                <input type="hidden" asp-for="ShowtimeId" />
                <button type="submit" class="btn btn-outline-success">Book Selected Seats</button>
            </form>
        </div>
    </div>
</div>
 *@

                                        @model MovieTicketBooking.Web.ViewModels.HomeViewModels.AvailableTicketViewModel

@{
    ViewData["Title"] = "Available Tickets";
}

<h1>@ViewData["Title"]</h1>

@if (Model.AvailableSeats.Any())
{
    <div class="container mt-5">
        <h2>@Model - Available Seats</h2>
        <hr />
        <form asp-action="BookTickets" asp-controller="Home" method="post">
            <div class="row">
                @foreach (var seat in Model.AvailableSeats)
                {
                    <div class="col-md-3">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" name="SelectedSeats" value="@seat" id="seat_@seat" />
                            <label class="form-check-label" for="seat_@seat">Seat #@seat</label>
                        </div>
                    </div>
                }
            </div>
            <div class="mt-3">
                <input type="hidden" asp-for="ShowtimeId" />
                <button type="submit" class="btn btn-outline-success">Book Selected Seats</button>
            </div>
        </form>
    </div>
}

